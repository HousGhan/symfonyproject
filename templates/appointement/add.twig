{% extends "base.html.twig" %}

{% block title %}Appointement/Add
{% endblock %}
{% block body %}
	<div class="container py-5">
		<div class="row justify-content-center">
			<div class="col-md-6">
				<div class="card rounded-4 overflow-hidden">
					<div class="card-header" style="background-color: #1c274c">
						<h4 class="mb-0 text-center text-white">Add Appointement for
							<span class="text-success">"{{patient.firstName ~ " " ~ patient.lastName}}"</span>
						</h4>
					</div>
					<div class="card-body">
						{{ form_start(form) }}
						<div class="form-floating mb-3">
							{{ form_widget(form.date) }}
							<label for="">
								Appointement Date
								<span class="text-danger fw-bold">
									*
									{% if form.date.vars.errors|length > 0 %}
										{{ form.date.vars.errors[0].message }}
									{% endif %}
								</span>
							</label>
						</div>
						<div class="text-center">
							<button type="submit" class="btn btn-success border-0 rounded-pill px-5 fw-medium dark-blue">
								Add
							</button>
						</div>
						{{ form_end(form) }}
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
